// Intentionally empty: this "package" ships no React components.
// The design system is the stylesheet (styles.css) + fonts/, extracted from
// the site's index.html by ../extract.mjs. window.AnaMoraisSiteStyles = {}.
export {};
